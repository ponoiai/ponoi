-- v1.247.0: реакция питомца профиля на клик (прыжок/кручение/эмодзи-всплеск/пульс) —
-- см. ProfilePet.tsx. Публичная колонка, как остальное оформление питомца (pet_*):
-- реакцию должны видеть все, кто смотрит на профиль, а не только владелец.
alter table public.profiles add column if not exists pet_reaction text;
